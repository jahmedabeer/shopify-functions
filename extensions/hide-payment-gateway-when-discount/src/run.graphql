query RunInput {
  cart {
    cost {
      subtotalAmount { amount }
      totalAmount { amount }
      totalTaxAmount { amount }
      totalDutyAmount { amount }
    }
  }
  paymentMethods {
    id
    name
  }
}